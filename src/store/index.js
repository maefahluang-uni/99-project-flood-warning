import Vue from 'vue';
import Vuex from 'vuex';

Vue.use(Vuex);

const store = new Vuex.Store({
  state: {
    // Define your state here
  },
  mutations: {
    // Define mutations
  },
  actions: {
    // Define actions
  },
  getters: {
    // Define getters
  }
});

export default store;
